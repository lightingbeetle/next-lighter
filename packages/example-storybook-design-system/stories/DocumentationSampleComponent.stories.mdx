import {Canvas, Meta, Story} from "@storybook/addon-docs";

import {DocumentationSampleComponent} from "./DocumentationSampleComponent";

# DocumentationSampleComponent
<Meta
    title="Example/DocumentationSampleComponent/Documentation"
    component={DocumentationSampleComponent}
    argTypes={{
        variant: {
            control: "select",
            options: ["small", "large"],
        }
    }}
/>

## Usage
Use this component to render a paragraph with custom text by passing it as `children` prop to the component.
Optionally specify `variant` prop to control the size of font in the paragraph.

export const Template = (args) => <DocumentationSampleComponent {...args} />;

## Variants
### Small
<Story
    name="small"
    args={{
        variant: "small",
        children: "This is a paragraph in small variant."
    }}
>
    {Template.bind({})}
</Story>

### Large
<Story
    name="large"
    args={{
        variant: "large",
        children: "This is a paragraph in large variant."
    }}
>
    {Template.bind({})}
</Story>